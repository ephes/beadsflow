#!/usr/bin/env bash
#
# Repo-local Beads defaults for beadsflow.
# Activate with: direnv allow

export BEADS_NO_DAEMON=1
export BEADS_DIR="$PWD/.beads"

# Optional local-only overrides (not committed)
if [[ -f .envrc.local ]]; then
  source .envrc.local
fi

